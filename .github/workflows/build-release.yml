name: Build and Release Binary

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag to create / update (e.g. v0.1.12)"
        required: true

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            artifact: beam-node-agent-linux
          - os: macos-latest
            artifact: beam-node-agent-macos

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install build dependencies
        run: |
          pip install --upgrade pip
          pip install pyinstaller aiohttp pyyaml "pydantic>=2.0"

      - name: Build binary
        run: |
          pyinstaller --onefile \
            --name ${{ matrix.artifact }} \
            --hidden-import pdb \
            --hidden-import bdb \
            --hidden-import cmd \
            --hidden-import code \
            --hidden-import cmath \
            --hidden-import pickletools \
            beam_node_agent/main.py

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: dist/${{ matrix.artifact }}

  release:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4

      - name: Determine release tag
        id: tag
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            echo "tag=${{ inputs.tag }}" >> "$GITHUB_OUTPUT"
          else
            echo "tag=${{ github.ref_name }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Create / update GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          name: "Beam Node Agent ${{ steps.tag.outputs.tag }}"
          body: |
            Automated binary build from commit ${{ github.sha }}.
          files: |
            beam-node-agent-linux/beam-node-agent-linux
            beam-node-agent-macos/beam-node-agent-macos
          make_latest: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
